# All optional. The code falls back to these exact values, so the site builds
# without them. Set them to point a deployment at a different dataset.
NEXT_PUBLIC_SANITY_PROJECT_ID=k8n4gdum
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2026-01-01
