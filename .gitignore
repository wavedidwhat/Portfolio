node_modules
.next
out
dist

# Secrets. `.env*.local` alone does NOT cover `.env.remote`, which holds the VPS
# SSH target — this repo is public, so be explicit.
.env
.env.*
!.env.example

# build artifacts
*.tsbuildinfo
next-env.d.ts

.DS_Store
*.log

# generated by scripts/migrate-to-sanity.mjs
sanity-export.ndjson
